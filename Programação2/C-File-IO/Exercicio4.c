/*
Faça a função int Remove_Item (arq* FILE, int chave) que remova o item
(registro) de chave “chave” do arquivo binário “arq”, caso tal item esteja
presente no arquivo. A função deve retornar “verdadeiro” caso a remoção do
item ocorra com sucesso; caso contrário, a função deve retornar “false”.
Considere que o arquivo em questão já se encontra aberto e que o item
(registro) do arquivo binário é formado pelos campos: chave (valor inteiro),
dado2 (cadeia de 30 caracteres) e dado3 (vetor com 4 valores reais).
*/
